# PyTorch installed separately (via setup.sh or system)
# Install: pip3 install -r requirements.txt -i https://pypi.org/simple/

transformers>=4.51.0  # Qwen3.5 architecture support requires >=4.51
datasets>=2.21.0
accelerate>=0.34.0
numpy>=1.26.0
scipy>=1.14.0
matplotlib>=3.9.0
tqdm>=4.66.0
pandas>=2.2.0
pyyaml>=6.0.1
huggingface_hub>=0.25.0

# Project-specific
trl>=0.12.0
peft>=0.12.0

# Qwen3.5 hybrid attention (optional — installed via setup.sh with --no-build-isolation)
# flash-linear-attention>=0.4.0
# causal-conv1d>=1.4.0
